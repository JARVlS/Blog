{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/content": "^2.3.0",
    "nuxt": "^3.0.0"
  },
  "dependencies": {
    "@vueuse/core": "^9.10.0",
    "@vueuse/router": "^9.10.0"
  }
}
